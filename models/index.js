const User = require('./user');
const Buyer = require('./buyer');
const School = require('./school');
const Products = require('./product')
const Transaction = require('./transaction')

module.exports = {
    User, Buyer, School, Products, Transaction
}